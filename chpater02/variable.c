#include <stdio.h>
int main()
{
    int a = 6;
    int b = 3;
    int c = 5;
    printf("this is the value of a = %d and here is the value of b = %c", a, b ); // %d,%f and %c are called formate specifier
    return 0;
}